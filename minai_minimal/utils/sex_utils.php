<?php
/**
 * Sex utilities for MinAI minimal (NSFW features disabled)
 */

// All NSFW/sex-related functions disabled in minimal version
// Placeholder functions to prevent errors

function GetSexContext($character) {
    return ""; // NSFW features disabled in minimal version
}

function IsSexuallyActive($character) {
    return false; // NSFW features disabled in minimal version
}

function GetArousalLevel($character) {
    return ""; // NSFW features disabled in minimal version
}

function GetSexActorValue($character, $key) {
    return ""; // NSFW features disabled in minimal version
}